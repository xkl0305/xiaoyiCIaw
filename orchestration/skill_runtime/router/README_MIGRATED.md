# Migrated compatibility directory

Canonical path: `orchestration/skill_runtime/router`.

Python files here are shims only. Original content was moved/copied to canonical and backed up under `legacy_readonly/V111_8_pre_final_cover/orchestration/router`.
