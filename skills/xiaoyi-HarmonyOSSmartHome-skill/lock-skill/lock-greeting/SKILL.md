---
name: lock-greeting
description: "智能门锁出门/回家留言设置技能。当用户提到出门留言、回家留言、添加留言、删除留言、修改留言、留言设置、对家人说、对XX说、提醒孩子/提醒家人时触发。用于控制门锁的语音播报功能，包括添加、删除、修改回家/出门留言。不支持查询操作。"
---

# 门锁出门/回家留言设置功能Skill

## 1. 必读项

本技能用于**控制**门锁的出门/回家留言设置，包括添加、删除、修改留言等操作。**不支持查询操作，查询操作请参考lock-info**。

### 服务ID（sid）
- **sid**: `homeGreeting`

### 核心参数速查

| 参数 | 含义 | 取值说明 |
|------|------|----------|
| `act` | 操作类型 | 0=增加, 1=删除, 2=修改 |
| `vid` | 留言ID | 回家:1-5, 出门:51-53 |
| `uid` | 用户ID | 回家:用户ID/"all"/"1,3,5", 出门:空字符串"" |
| `rpt` | 重复类型 | 0=仅一次, 1=每天, 2=每周, 3=每月, 4=每年, 5=指定日期 |
| `rd` | 周期 | 与rpt配合：每天=""，按周="1,2,3,4,5,6,7"，每月="21"，每年="1,2" |
| `ve` | 留言内容 | 自定义文本 |
| `tb` | 音色 | 0=小艺女声, 1=小艺少女, 3=小艺童声, 4=小艺男声 |
| `st/et` | 时间范围 | 格式HH:mm，24小时制 |
| `type` | 语料类型 | 1=文字留言, 2=录音留言 |
| `switch` | 播放开关 | 0=关闭, 1=开启 |

### 重要说明
- 回家留言和出门留言是不同的功能，执行时注意根据用户要求区分
- 留言的音量大小可以在音量设置-留言/天气提醒音量中进行设置
- 修改留言后，会在下次【门锁在线】时生效，以下任一情况会让【门锁在线】：
  1. 开关门
  2. 产生逗留录像或图片
  3. 有人按门铃
  4. 开启实时视频

### VID说明
| 类型 | VID范围 | 最大数量 |
|------|---------|----------|
| 回家留言 | 1, 2, 3, 4, 5 | 5条 |
| 出门留言 | 51, 52, 53 | 3条 |

### 用户说明
- 用户开门时，门锁可以区分回家的用户，因此可以针对用户设置回家留言
- 用户出门时，门锁无法区分离家的用户，因此出门留言的用户均设置为所有用户（uid=""空字符串）

## 2. 触发关键字

当用户提到以下关键词时，应加载本技能：
- 出门留言、回家留言
- 留言设置、添加留言、删除留言、修改留言
- 门锁语音提醒、对家人说、对XX说
- 提醒孩子、提醒家人

## 3. 操作流程

### 3.1 添加留言流程 (act=0)

```
1. 确定留言类型 → 回家(vid 1-5) 或 出门(vid 51-53)
2. 查找可用VID → 从小到大遍历未占用的VID
3. 确定目标用户 → 回家可指定用户/全部，出门的uid必为空
4. 确定重复规则 → rpt + rd 参数组合
5. 选择音色 → tb参数（默认0小艺女声）
6. 构造data发送 → 无需用户确认直接执行
```

### 3.2 删除留言流程 (act=1)

```
1. 查询现有留言 → 确定要删除的vid（参考lock-info）
2. 获取用户确认 → 删除操作有破坏性，必须确认
3. 构造删除命令 → 只需vid+act，其他字段可省略
4. 发送执行 → 以vid唯一标识删除
```

### 3.3 修改留言流程 (act=2)

```
1. 查询现有留言 → 确定要修改的vid
2. 获取用户确认 → 修改操作需用户确认
3. 确定变更内容 → 内容/用户/时间/音色等
4. 构造修改命令 → 完整保留原vid，修改目标字段
5. 发送执行
```

## 4. 命令格式

```bash
node common-skill/bin/smarthome-claw.js control_device \
  --dev-id "设备ID" \
  --prod-id "产品ID" \
  --operation "POST" \
  --sid "homeGreeting" \
  --data '{"cfg":[{留言配置}],"ts":"时间戳"}' \
  --verbose
```

## 5. 完整案例

### 5.1 添加回家留言 - 针对特定用户

> **场景**：用户"ZTT"添加回家留言："宝贝，回家后记得写作业"
> **VID分配**：vid=2（假设vid=1已被占用）
> **用户**：uid=3（ZTT）
> **重复**：每天播放

**执行命令：**
```bash
node common-skill/bin/smarthome-claw.js control_device \
  --dev-id "xxx" \
  --prod-id "xxx" \
  --operation "POST" \
  --sid "homeGreeting" \
  --data '{"cfg":[{"vid":2,"fid":"","uid":"3","st":"07:00","rd":"","act":0,"rpt":1,"type":1,"tb":0,"switch":1,"et":"24:00","ve":"宝贝，回家后记得写作业"}],"ts":"20260621T160722Z576"}' \
  --verbose
```

---

### 5.2 添加回家留言 - 全部用户

> **场景**：添加面向所有家人的回家留言："开好运，迎新年"
> **VID分配**：vid=2
> **用户**：uid=all（全部用户）

**执行命令：**
```bash
node common-skill/bin/smarthome-claw.js control_device \
  --dev-id "xxx" \
  --prod-id "xxx" \
  --operation "POST" \
  --sid "homeGreeting" \
  --data '{"cfg":[{"vid":2,"fid":"","uid":"all","st":"07:00","rd":"1,2,3,4,5,6,7","act":0,"rpt":1,"type":1,"tb":0,"switch":1,"et":"24:00","ve":"开好运，迎新年"}],"ts":"20260621T160722Z576"}' \
  --verbose
```

---

### 5.3 添加回家留言 - 指定日期+音色

> **场景**：针对用户3添加留言："财运旺，万事顺"
> **VID分配**：vid=3
> **用户**：uid="3"
> **音色**：小艺童声(tb=3)
> **周期**：每月21号(rpt=3, rd=21)

**执行命令：**
```bash
node common-skill/bin/smarthome-claw.js control_device \
  --dev-id "xxx" \
  --prod-id "xxx" \
  --operation "POST" \
  --sid "homeGreeting" \
  --data '{"cfg":[{"vid":3,"fid":"","uid":"3","st":"07:00","rd":"21","act":0,"rpt":3,"type":1,"tb":3,"switch":1,"et":"24:00","ve":"财运旺，万事顺"}],"ts":"20260621T160739Z891"}' \
  --verbose
```

---

### 5.4 修改回家留言

> **场景**：将vid=3的留言内容改为"展宏图，开新局"，用户改为2和3，周期改为每周一二三
> **操作**：act=2（修改）

**执行命令：**
```bash
node common-skill/bin/smarthome-claw.js control_device \
  --dev-id "xxx" \
  --prod-id "xxx" \
  --operation "POST" \
  --sid "homeGreeting" \
  --data '{"cfg":[{"vid":3,"uid":"2,3","st":"07:00","rd":"1,2,3","act":2,"rpt":2,"type":1,"tb":1,"switch":1,"et":"24:00","ve":"展宏图，开新局"}],"ts":"20260621T160804Z354"}' \
  --verbose
```

---

### 5.5 修改出门留言

> **场景**：修改vid=52的出门留言："月上柳梢头，人约黄昏后"
> **VID分配**：vid=52（出门留言）
> **用户**：uid=""（出门无法区分用户，固定空字符串）
> **音色**：小艺男声(tb=4)
> **周期**：每月21号

**执行命令：**
```bash
node common-skill/bin/smarthome-claw.js control_device \
  --dev-id "xxx" \
  --prod-id "xxx" \
  --operation "POST" \
  --sid "homeGreeting" \
  --data '{"cfg":[{"vid":52,"uid":"","st":"07:00","rd":"21","act":2,"rpt":3,"type":1,"tb":4,"switch":1,"et":"24:00","ve":"月上柳梢头，人约黄昏后"}],"ts":"20260621T160836Z518"}' \
  --verbose
```

---

### 5.6 删除回家留言

> **场景**：删除vid=2的回家留言
> **操作**：act=1（删除）
> **注意**：删除时ve内容可任意，系统以vid为唯一标识

**执行命令：**
```bash
node common-skill/bin/smarthome-claw.js control_device \
  --dev-id "xxx" \
  --prod-id "xxx" \
  --operation "POST" \
  --sid "homeGreeting" \
  --data '{"cfg":[{"vid":2,"uid":"all","st":"07:00","rd":"1,2,3,4,5,6,7","act":1,"rpt":1,"type":1,"tb":0,"switch":1,"et":"24:00","ve":"开好运，迎新年"}],"ts":"20260621T160716Z585"}' \
  --verbose
```

---

### 5.7 删除出门留言

> **场景**：删除vid=51的出门留言
> **操作**：act=1（删除）
> **用户**：uid=""（出门留言固定空字符串）

**执行命令：**
```bash
node common-skill/bin/smarthome-claw.js control_device \
  --dev-id "xxx" \
  --prod-id "xxx" \
  --operation "POST" \
  --sid "homeGreeting" \
  --data '{"cfg":[{"vid":51,"uid":"","st":"07:00","rd":"","act":1,"rpt":1,"type":1,"tb":0,"switch":1,"et":"24:00","ve":"今天比较冷，出门多穿点"}],"ts":"20260621T160853Z781"}' \
  --verbose
```

## 6. 核心规范 (Critical Rules)

1. **增量更新原则**：`cfg`数组**仅包含**本次操作的留言配置，**严禁**携带其他留言数据
2. **VID唯一性**：已占用的VID不能重复使用，若满额需先删除再添加
3. **用户确认要求**：
   - 添加留言 → 空位充足时无需确认
   - 修改/删除留言 → **必须用户确认**
4. **uid格式**：
   - 回家-指定用户：`"3"`
   - 回家-全部用户：`"all"`
   - 回家-部分用户：`"1,4,5"`
   - 出门留言 → **固定空字符串 `""`**
5. **删除命令特点**：系统仅依据vid识别删除目标，其他字段可任意值

## 7. 注意事项

- 本技能仅支持控制操作，**不支持查询**
- **删除/修改留言必须用户确认**后执行
- 门锁最多存在5条回家留言(vid 1-5)，3条出门留言(vid 51-53)
- 留言生效需门锁在线（开关门/逗留录像/按门铃/开启实时视频）

(End of file)