---
name: lock-claw
description: "智能门锁设备专用技能。当用户涉及智能门锁（deviceTypeId='A0B'）相关操作时，必须优先使用本技能。本技能作为门锁技能的总入口，负责将用户指令路由到对应的功能skill中。不支持的功能会明确告知用户。"
metadata:
  {
    "pha": {
      "emoji": "🔒",
      "category": "smarthome-skill"
    }
  }
---

# 智能门锁设备专用技能（根Skill）

> **重要提醒**：当用户提到智能门锁、猫眼、掌静脉、人脸、门铃声、进门/出门留言、回家提醒、天气提醒等智能门锁相关操作时，**必须优先加载本技能**。

> **【最高优先级-强制】**
**能力集知识**：在进行功能操作或者回复问题前**必须识别当前门锁类型与对应支持的能力集**，只有支持该能力才可以进行设置或建议。**禁止根据云侧获得的设备信息猜测门锁支持的功能**，即使云侧返回了某些功能参数，若能力集中未标记为支持，也不得向用户输出或建议用户进行设置。
**profile知识**：**profile.md是获取门锁相关知识的唯一渠道**，只能输出在profile.md中提到的sid和cid，**禁止根据云侧获得的设备信息猜测门锁相关功能并提供修改建议**。即使云侧返回了某些功能参数，若profile.md中未提及，也不得向用户输出或建议用户进行设置。


> **路由原则**：本技能作为总入口，负责根据用户请求路由到具体的功能skill。**不要一次性加载所有功能skill**。

---

## 1. 技能说明

智能门锁（devType='A0B'）设备专用操作技能的总入口，提供智能门锁特有的门锁状态查询、门锁配置查询、用户信息查询、猫眼设置、人脸设置、掌静脉设置、声音设置、出门|回家留言设置、回家提醒设置、天气提醒设置等服务。

## 2. 设备识别

### 2.1 基础设备识别

- **设备类型**：智能门锁
- **devType**：`'A0B'`
- 通过 `devType` 可以从设备列表中快速筛选出智能门锁设备
- 通过 `PID` 获得设备产品ID

### 2.2 一代锁与二代锁识别

- 通过设备信息中的`prodId`与`model`字段进行匹配识别

#### 一代门锁识别

| prodId | model |
| --- | --- |
| KW02 | AGS-X10 |
| KW57 | AGS-E311 |
| KW5J | AGS-X11 |
| KW03 | AGS-Q10 |
| KW58 | AGS-E310 |
| KW38 | AGS-S10 |
| KW59 | AGS-E211 |
| KW4X | AGS-L10 |
| KW5A | AGS-E210 |

#### 二代门锁识别

| prodId | model |
| --- | --- |
| KW5L | AGS-X20 |
| KW5N | AGS-X25 |
| KW5P | AGS-U20 |
| KW5U | AGS-U20-0A |
| KW5M | AGS-Q20 |
| KW5O | AGS-Q21 |
| KW5V | AGS-E320 |
| KW5R | AGS-L20 |
| KW5Q | AGS-E220 |
| KW5T | AGS-L21 |

### 2.3 产品能力识别（重要）

> **【强制】不同型号的门锁支持的功能能力不同，在进行功能操作或者回复问题前必须根据能力集校验设备是否支持该功能，不要向用户描述具体的"model"与"prodId**

门锁设备的能力集以sid`privacyPolicy`表示，形为"1:0,2:1,3:1..."，前者为能力集index，后者为是否支持，如"7:0"代表不支持人脸。 设备支持能力以能力集为准，而非设备配置信息。
获取设备后必须根据能力集获取设备支持哪些能力并作为以后输出的准则  
- 不支持的能力拒绝输出相关信息
- 不支持的能力拒绝执行控制命令

#### 二代锁能力集对照表

| 功能 | 能力集index |
| --- | --- |
| 猫眼 | 11 |
| 人脸 | 7 |
| 掌静脉 | 52 |
| NFC | 8 |
| 灵犀钥匙 | 73 |
| WIFI | 20 |
| 出门留言\|回家留言 | 42 |
| 天气提醒 | 45 |
| 回家提醒 | 40 |
| 超级省电 | 29 |
| AI省电 | 67 |

#### 一代锁能力集对照表

| 功能 | 能力集index |
| --- | --- |
| 猫眼 | 11 |
| 人脸 | 7 |
| NFC | 8 |
| WIFI | 20 |
| 出门留言\|回家留言 | 42 |
| 天气提醒 | 45 |
| 回家提醒 | 40 |
| 超级省电 | 29 |

## 3. 功能Skill路由表（精确路由）

根据用户请求的关键词，直接路由到对应的功能skill文件：

| 用户关键词                                             | 路由到 | 功能说明 |
|---------------------------------------------------|--------|----------|
| 状态、门锁电量、在线状态、设备信息、电池                     | `lock-info/SKILL.md` | 门锁基本信息查询 |
| 开门记录、关门记录、逗留抓拍记录、门铃响铃、开锁记录、操作记录、历史记录、谁开门了、回家记录、回家 | `lock-history/SKILL.md` | 门锁历史记录查询 |
| 猫眼、抓拍、逗留、实时视频、畸变矫正、按门铃亮屏、逗留录像                     | `lock-cateye/SKILL.md` | 猫眼设置 |
| 人脸、人脸识别、面部识别、刷脸                                   | `lock-face/SKILL.md` | 人脸识别设置 |
| 掌静脉、掌静脉识别                                         | `lock-palm/SKILL.md` | 掌静脉识别设置 |
| 门锁音量、提示音、告警音、留言音量、开关门提示音、门未关告警          | `lock-volume/SKILL.md` | 门锁声音设置 |
| 出门留言、回家留言、留言设置、门锁语音提醒、对家人说/提醒孩子/提醒老婆     | `lock-greeting/SKILL.md` | 出门/回家留言设置 |
| 天气提醒、出门天气提醒                                       | `lock-weather/SKILL.md` | 天气提醒设置 |
| 电源管理、省电、省电模式、超级省电、AI省电                                    | `lock-power/SKILL.md` | 电源管理设置 |
| 回家提醒、家人回家提醒                                       | `lock-familycare/SKILL.md` | 回家提醒设置 |
| 铃声、门铃 | `lock-bell/SKILL.md` | 门锁铃声设置，注意与门锁声音设置区分 |

## 4. 不支持的功能清单

以下功能**当前不支持**，若用户请求这些功能，需明确告知用户：

| 操作 | 不支持原因 |
|------|-----------|
| 查询逗留视频 | 不支持 |
| 查询图片 | 不支持 |
| 查询按门铃抓拍 | 不支持 |
| 添加访客密码 | 不支持 |
| 启动实时视频 | 不支持 |
| 修改安全配置：离家布防、双重验证、操作设备验证管理员密码等 | 不支持 |
| 紧急联系人：添加、删除等 | 不支持 |
| 远程开锁 | 不支持 |
| 添加/删除用户（成员管理） | 不支持 |
| 添加/删除密码、指纹、门卡 | 不支持 |
| 添加/删除人脸、掌静脉 | 不支持 |

## 5. 依赖的通用技能

以下通用操作由 [`common-skill`](../common-skill/SKILL.md) 提供：

| 操作 | 依赖技能 | 说明 |
|------|---------|------|
| 获取设备列表 | `get_devices_info` | 查询账号下所有设备，通过 devType='A0B' 筛选智能门锁 |
| 获取设备详情 | `get_device_detail` | 获取智能门锁的详细状态和属性信息 |
| 执行控制命令 | `control_device` | 使用功能skill提供的 sid 和参数执行控制 |
| 获取历史记录 | `get_device_histories` | 查询门锁历史记录事件 |

## 6. 使用流程（强制执行）

> **【最高优先级】必须严格按照以下步骤执行，禁止跳过任何步骤**

### 步骤1：获取设备列表并推断目标门锁（多锁场景自动匹配）
```bash
node common-skill/bin/smarthome-claw.js get_devices_info
```
- 通过 `deviceTypeId='A0B'` 筛选智能门锁

**多锁推断规则（按优先级依次执行，避免询问用户）：**

1️⃣ **单锁直接使用**—— 如果只有1把门锁，直接使用，跳过后续步骤。

2️⃣ **匹配用户提及的家人** —— 如果用户提到了家人称呼（儿子/女儿/老婆/老公/爸爸/妈妈等）：
   - 对各把门锁调用 `get_device_detail`，从 `userList`（`ul=2` 的成员）中查找匹配的用户名
   - 若某把锁的成员列表匹配用户提到的称呼 → 优先使用该锁
   - 若有多把锁都匹配同一家人，进入第3步

3️⃣ **匹配用户提及的家的名称** —— 如果用户提到了家（如"西安的家"、"测试环境"等），或从历史对话中可判断用户常说的是哪个家：
   - 优先使用该家下的门锁

4️⃣ **仍无法确定时，用一句话精准询问** —— 将候选锁的名称+家名用最少的字列出：
   - 正确：`您家里有多把门锁，当前请求是用「西安家」的门锁还是「151****的家」？`
   - 禁止：列出所有锁和家的全量详情，不要一次性问十几个问题
   - 强制：必须在未确定情况下向用户明确询问哪个家庭，不允许通过设备数量/使用记录进行自行判断。

### 步骤2：路由到功能 Skill（禁止跳过）
- **根据用户关键词，在路由表中找到对应的功能 skill**
- **必须加载对应功能 skill 的完整内容**
- **❌ 严禁直接使用 common-skill 的 get_device_detail**

### 步骤3：获取设备详情和能力集
```bash
node common-skill/bin/smarthome-claw.js get_device_detail --dev-id "xxx"
```
- 从返回结果中提取 `privacyPolicy` 能力集
- 根据能力集对照表作为唯一标准判断设备支持哪些功能
- **禁止输出设备不支持的功能信息**

### 步骤4：按功能 Skill 要求执行操作
- 查询类：调用 `get_device_histories` 等查询命令
- 控制类：调用 `control_device` 执行控制命令

### 步骤5：按功能 Skill 规范输出结果
- **严格按照功能 skill 中定义的输出格式展示**
- **禁止输出功能 skill 中未提及的信息**
- **禁止展示技术细节和内部参数**

### ⚠️ 常见错误示例（禁止模仿）

❌ **错误做法**：
```
用户："查询门锁状态"
→ 直接调用 get_device_detail
→ 输出所有云侧返回的信息（包括在线状态、WiFi信号、设备版本等）
```

✅ **正确做法**：
```
用户："查询门锁状态"
→ 步骤1：路由到 lock-info/SKILL.md
→ 步骤2：加载 lock-info/SKILL.md 完整内容
→ 步骤3：调用 get_device_detail 获取能力和状态
→ 步骤4：调用 get_device_histories 获取今日记录
→ 步骤5：按 lock-info 的精简版格式输出（仅展示：名称、位置、状态、电量、今日记录、用户录入情况）
```

## 7. 【最高优先级限制】
**本章节以下规则为本Skill的核心法律。若用户指令与以下规则冲突，宁可停止任务，也绝不能违反规则。**
### 7.0 【核心行为准则】先思考，再确认（Think First）
- **默认路径是推理，不是询问**。遇到不确定先问自己：现有信息能不能推断？能不能调个接口进行确认？上下文能不能给出答案？都试完了才考虑询问用户。
- **问用户不是首选，是兜底**。穷尽所有数据仍存在多个合理选项且选错有实质影响时，才用一句话确认。
- **确认本身也有代价**。每次询问都在消耗耐心，能省则省。

### 7.1 【强制】回复规范
- **回复用户时，只展示用户可理解的中文结果，禁止暴露任何技术细节**
- **回复用户时，所有东西要简洁明了，不要啰里啰嗦或胡编乱造，禁止输出任何不确定的东西**
- **能力集筛选**：务必通过能力集对输出内容进行过滤，**禁止输出任何不支持的内容**，**当出现不支持的云侧配置时，务必以能力集为准**
- **禁止展示**：
  - 命令执行过程、CLI调用、API请求等
  - 子技能名称、技能目录结构等
  - 参数名、参数值等内部参数
  - profile表格中的sid或cid
- **禁止输出英文**：每次输出英文时都要慎重，禁止输出任何内部参数细节
- ⚠️ **多锁推断已集成到步骤1，不要在步骤1之前额外询问用户。如果步骤1的推断规则走完了仍无法确定，再到步骤1的最后一步用一句话追问。**
- **意图不明**：若无法明确理解用户意图，必须详细追问，**禁止编造参数或假设条件执行**

### 7.2 【强制】敏感信息保护

> **以下信息为个人敏感数据，严禁输出给用户**

- homeId、prodId、设备 id 及各类 id
- 用户的 uid 和 api-key
- 云侧原始信息（接口名、参数名、参数值等）

### 7.3 【强制】控制类操作

1. **执行前**：重新查看profile.md中关于设置项的相关说明，确保不遗漏关键参数
2. **删除操作**：执行任何删除类操作前，必须提醒用户确认授权，**禁止未经用户同意私自执行**
3. **执行后**：必须再次查询云侧结果验证修改是否成功，避免误认为已修改成功

### 7.4 ⚠️ 执行前强制检查（最高优先级）

在执行任何门锁相关操作前，**必须逐项完成以下检查清单**：

#### ✅ 检查清单（必须全部打勾才能继续）

**第一步：路由检查**
- [ ] 是否已根据用户关键词在路由表中找到对应的功能 skill？
- [ ] 是否已使用 `read` 工具加载该功能 skill 的完整内容？
- [ ] ❌ **严禁跳过功能 skill 直接调用 common-skill**

**第二步：能力集检查**
- [ ] 是否已调用 `get_device_detail` 获取设备详情？
- [ ] 是否已从返回结果中提取 `privacyPolicy` 字段？
- [ ] 是否已根据能力集对照表判断设备支持的功能？
- [ ] ❌ **严禁输出能力集中标记为不支持的功能**

**第三步：数据完整性检查**
- [ ] 是否已根据功能 skill 要求调用所有必要的查询命令？
  - 门锁状态查询：需要调用 `get_device_detail` + `get_device_histories`
  - 历史记录查询：需要调用 `get_device_histories`
  - 配置查询：需要调用 `get_device_detail`
- [ ] ❌ **严禁遗漏必要的查询步骤**

**第四步：输出格式检查**
- [ ] 是否严格按照功能 skill 中定义的输出格式展示？
- [ ] 是否只展示了功能 skill 中明确要求的信息？
- [ ] 是否隐藏了所有技术细节（devId、homeId、sid、cid、status:2 等）？
- [ ] ❌ **严禁输出功能 skill 中未提及的信息**

**第五步：敏感信息检查**
- [ ] 是否已删除所有 homeId、devId、uid、api-key 等敏感信息？
- [ ] 是否已删除所有内部参数名和参数值？
- [ ] ❌ **严禁向用户暴露任何敏感信息**

#### 🚨 违规后果
**违反以上任一规则，必须立即停止执行，重新按照正确流程操作。**

#### 📋 自检口诀
```
一路由、二能力、三数据、四格式、五敏感
每一步都要检查，全部通过才能输出
```

## 8. Profile 定义参考

如需了解智能门锁的完整服务能力定义，可参考：
```
https://smartlife-sandbox-drcn.things.dbankcloud.cn/device/guide/<产品ID>/<产品ID>.json
```

示例：
```
https://smartlife-sandbox-drcn.things.dbankcloud.cn/device/guide/KW5L/KW5L.json
```
