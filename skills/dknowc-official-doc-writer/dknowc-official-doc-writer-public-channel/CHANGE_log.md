# 更新日志

## [2.6.1] - 2026-04-09

### 搜索 API Key 更新

- 更新 `config.ini` 中的深知可信搜索 API Key

---

## [2.6.0] - 2026-04-09

### SKILL.md 结构瘦身 + 步骤链重写

**背景：** SKILL.md 膨胀至 817 行，AI 注意力分散导致部分规则未严格执行。需要将详细模板/示例与核心行为规则分离，并将文档式描述改为严格的步骤链执行流程。

#### 一、内容拆分

所有行为规则保留在 SKILL.md 并精简表述；格式模板、输出示例、参数说明等摘到独立 reference 文件，按需引用。

**新增文件：**
- `reference/search_guide.md` — 搜索结果分类格式、参考范文召回流程、素材交付模板、末尾素材使用情况示例
- `reference/output_guide.md` — Markdown格式规则、红头文件参数、版本号规则、密级处理、附录A-D

#### 二、步骤链重写

将章节描述式（"3.1是做什么的"）改为严格的步骤链式执行流程（步骤1→2→3...），每个步骤包含：
- **进入条件：** 上一步必须完成什么才能进入本步
- **必须执行：** 本步必须完成的动作
- **分支/下一步：** 完成后去哪

完整流程：步骤1(判断搜索) → 步骤2(设计搜索方案) → 步骤3(执行搜索) → 步骤4(结果分类) → 步骤5(自检) → 步骤6(用户确认) → 步骤7(范文召回+素材交付) → 步骤8(识别文种) → 步骤9(大纲确认) → 步骤10(撰写) → 步骤11(生成前检查) → 步骤12(生成Word) → 步骤13(输出)

**跳步路径：** 步骤1判断不需要搜索 → 跳至步骤8

#### 三、行数变化

- 原 SKILL.md：817行
- 瘦身后：394行
- 步骤链重写后：342行
- 总压缩：**58%**

#### 四、步骤链细节优化

- **步骤9大纲确认：** 从"AI自行判断"改为明确的场景分类，执行过搜索的公文必须出大纲，未搜索+结构简单的可直接撰写
- **步骤10撰写：** 明确要求【素材使用情况】前必须写 `[分页符]` 另起一页，并在 SKILL.md 中直接展示完整输出格式示例
- **步骤10撰写：** 明确要求知识专库链接必须写入 Word 文档，链接 URL 必须独占一行以 `https://` 开头

#### 五、format_document.py 排版修复

**问题：** 生成的 Word 文档中，素材使用情况没有另起一页，知识专库链接排版混乱（标签行有缩进、URL 行没缩进），链接不可点击。

**修复：**
- 新增 `in_kb_section` 标志位，检测到 `【知识专库链接】` 后，区域内所有内容强制靠左排版、不缩进
- 区域内 URL 行统一走 `add_hyperlink()` 生成蓝色可点击超链接
- URL 匹配从精确匹配改为 `re.search` 模糊匹配，兼容更多格式

#### 未改动

- 搜索策略构造规则（2.5.1优化内容保持不变）
- 搜索脚本 dkag_search.py、merge_search_results.py 未改动
- 配置文件、标准文件均未改动

---

## [2.5.1] - 2026-04-09

### 搜索策略构造规则优化（3.2节）

**背景：** 原3.2节"方案设计指导"为5个维度平铺罗列（政策依据、参考案例、数据支撑、时间范围、规范性文件），缺乏层级化构造指导，导致AI搜索方案设计时各维度内部缺少分层覆盖。

**改动：** 将扁平维度清单替换为"层级化搜索框架"，核心思路是每个主题维度内部按行政层级逐级拆分搜索。

#### 新增搜索策略构造框架

- **政策依据**：按行政层级从上到下逐级搜索（国家级→省级→市级），不跳级不混搜，每个层级单独构造query
- **数据支撑**：本级整体数据 + 向下钻取到下属行政区划，根据需要为各区县分别构造query
- **参考案例**：横向（同级可比地区做法）+ 纵向（本地区历年同类工作数据，用于对比分析）
- **附加维度**：时间范围（`--time`）、规范性文件（`--policy`）、地域参数（`--area`）合并为按需叠加项

#### 删除的内容

- 原"方案设计指导"5点平铺清单
- 原"地域规则"独立子节（合并到附加维度中）

#### 未改动

- 搜索词构造规则（自然语言原则不变）
- 方案输出格式不变

---

## [2.5.0] - 2026-03-25

### 素材交付与参考范文机制重构

**背景：** 四模型写作测试发现，素材一股脑丢给写作模型、不标注用途，导致写作效果不佳。同时发现深知搜索 `--full` 全文召回与 `--clean` 清洗函数存在冲突。

#### 一、参考范文召回（3.7节重写）

**旧流程：** "全文召回"——获取文章全文，没有区分"写作素材"和"成文参考"的用途

**新流程："参考范文召回"**
- 明确定位：普通搜索收集"写作素材"，参考范文召回获取"成文参考"（结构和逻辑）
- 用文章标题做query，**不加 `--clean`**（加了会丢弃全文字段）
- 从API返回的多篇结果中按标题精确匹配，只提取目标文章的"全文"字段
- 向用户推荐时说明参考价值（同类公文、结构完整、深度分析）
- 限制最多2篇，尽量控制在1篇
- 提取失败时告知用户，允许重新选择或跳过

#### 二、新增素材交付准备（3.8节）

**在用户确认素材、参考范文召回完成后、进入撰写前，AI将所有素材整理成结构化的"素材使用指引"**

- 写作素材按类型分组（政策依据、数据支撑、参考案例、表述参考），每组标注用途（→用于：正文某部分）
- 参考范文单独列出，标注参考价值、注意事项、全文内容
- 写作素材和参考范文严格分开，告诉写作模型各用来做什么
- 不要一股脑丢原始素材，经过分类和用途标注后再传给写作模型

#### 三、引用规范重写（5.0节）

**旧流程：** 正文加 `[^N^]` 引用标记，末尾列【参考来源】

**新流程：**
- 正文中**不加任何引用标记**，保持正文干净
- 所有素材使用情况统一在末尾【素材使用情况】中标注
- 【素材使用情况】分为两个子节：
  - 写作素材：每条标注"→ 用于：正文某部分"
  - 参考范文：标注"→ 参考：学习了哪些方面"（如有全文召回）
- 参考范文只学结构不照搬，旧数据需用写作素材中的新数据替换

#### 四、配套调整

- 6.3生成前检查：去掉引用标记检查，改为素材使用情况检查
- 3.4素材分类：去掉引用编号相关描述，素材分类表中"是否引用"改为"末尾标注"
- 3.4素材分类标题：去掉"需引用"字样

#### 五、`--full` 参数验证结论

| 发现 | 结论 |
|------|------|
| `--full` + `--clean` | 全文字段被清洗函数丢弃 |
| `--full` 不加 `--clean` | 全文字段正常返回 |
| API返回多篇全文 | 需按标题精确匹配，只提取目标文章 |
| 搜索引擎排序 | 时间优先，旧文章（如2024年）排得很后 |
| 全文召回token消耗 | 19篇全文约6.7万字符，无关全文会干扰写作质量和模型注意力 |

---

## [2.4.4] - 2026-03-20

### 知识专库链接优化

- 知识专库链接（yun.dknowc.cn）不再按正文格式排版，去掉首行缩进
- 链接改为 Word 可点击超链接（蓝色下划线），支持 Ctrl+点击直接跳转浏览器

---

## [2.4.3] - 2026-03-20

### 标准文件重组与格式修正

#### 标准文件排序
- 按《党政机关公文处理工作条例》法定顺序重排 01-10：报告、请示、批复、通知、意见、函、纪要、通报、通告、公告
- 非法定文种后移：复函(11-12)、提醒函(13)、通用公文(14)、事务文书(15)
- 同步更新 SKILL.md 和 00_索引.md 中的文件引用

#### 页边距修正（客户反馈）
- **问题：** format.json 和标准文件中页边距统一为 25mm，数据来源为文件办理表格式要求，不适用于公文正文
- **修复：** format.json 改为国标 GB/T 9704（上37mm 下35mm 左28mm 右26mm）
- format_document.py 的 get_page_margin() 改为支持四边分别设置

#### 标准文件精简
- 14_通用公文_标准.md：去掉重复的字体字号行距页边距等格式细节，只留文种识别和结构说明
- 15_事务文书_模板.md：同上

#### Emoji 清理
- 00_索引.md、06_函_标准.md、SKILL.md 中所有 emoji 符号已移除

---

## [2.4.2] - 2026-03-19

### 线上测试问题修复

#### 问题一：搜索地域策略执行错误
- **现象：** AI按文种判断一次性搜索了5个地域，未走"先搜→自检→按需补搜"流程
- **修复：** 去掉按公文类型区分单地域/多地域的规则，统一为默认只搜用户提到的地域

#### 问题二：多地域搜索结果未按地域隔离
- **现象：** 各地域结果混合分类，外省政策被引用为本省政策依据
- **修复：** 素材分类新增来源限制——政策依据/数据支撑仅允许国家级和本省，参考案例仅允许其他省市

#### 问题三：参考案例不在自检范围内
- **现象：** 单地域搜索时参考案例天然缺失，AI无法补搜
- **修复：** 参考案例纳入自检范围，不足时AI自主搜索1-2个相关省市补充

#### 问题四：函件写作规范问题（客户反馈）
- **现象：** 征求意见函写了"逾期视为无意见"，联系人写了邮箱
- **修复：** `reference/standards/14_函_标准.md` 新增第四章——禁止写"逾期视为无意见"、禁止写邮箱

---

## [2.4.1] - 2026-03-19

### 深知搜索流程优化

#### SKILL.md 改动

**3.1 搜索词构造规则重写：**
- 从"标准公文标题提取"改为"AI从用户原始输入中清洗关键词"
- 定义构造标准：保留核心业务词，去掉写作指令词、文种词、连接废话、人称代词、目的描述
- 控制10-30字，附质量自检规则

**3.3 搜索结果分类新增知识专库链接即时发送：**
- 搜索完成后立即在对话中发送知识专库链接给用户
- 用户可点开查看完整检索结果，辅助判断素材充分性

**3.4 新增素材充分性自检（AI自动执行）：**
- AI自动评估各维度素材覆盖度，不足则自主构造针对性query补搜
- 每个维度最多补搜1次，累计补搜不超过3次
- 超过限制停止补搜，进入用户确认环节

**3.4 新增policyFiles按需补充：**
- AI撰写时发现政策引用缺文件全称或发文字号，用文章标题带 `--policy` 再搜一次
- 不需用户确认，AI自主判断执行

**3.5 用户确认环节重写：**
- AI展示素材覆盖情况 + 知识专库链接
- 向用户确认两件事：素材是否充分、是否需要上传参考文件
- 用户确认后才进入撰写

#### dkag_search.py 改动

- 新增 `--policy` 命令行参数，请求时携带 `policy: true`
- `clean_dkag_response` 保留 `policyFiles` 字段（之前被丢弃）

---

## [2.4.0] - 2026-03-19

### 会议纪要红头支持 + 空行清理 + 排版修复

#### format_document.py 修复

**问题：** `is_recipient()` 函数判断主送机关的逻辑过于宽泛，超过50字的长段落（如会议纪要的会议概况段）也会被误判为主送机关，导致缺少首行缩进。

**修复：** 新增长度限制，超过50字的段落直接排除，不作为主送机关处理。

#### 新增功能：会议纪要模板支持

**背景：** 广东省政数局公文格式标准定义了9种格式样本，之前只支持3种（上行文、下行文、函件），本次新增会议纪要系列。

#### 新增模板：
- `工作会议纪要-红头.docx` — WPS文本框格式（非表格），占位符：发文机关、印发单位、成文日期
- `工作会议纪要-表尾.docx` — 2个表格（出席列席 + 印发行），占位符：出席人员、列席人员、印发单位、成文日期
- `党组会议纪要-红头.docx` — 表格格式，占位符：发文机关、纪要编号、印发单位、成文日期
- `党组会议纪要-表尾.docx` — 2个表格（出席列席 + 抄送印发），占位符：出席人员、列席人员、抄送、印发单位、成文日期
- `局长办公会议纪要-红头.docx` — 表格格式，占位符：发文机关、纪要编号、印发单位、成文日期
- `局长办公会议纪要-表尾.docx` — 2个表格（出席列席 + 印发行），占位符：出席人员、列席人员、印发单位、成文日期

**新增TEMPLATE_MAP映射：**
- 工作会议纪要 → 工作会议纪要-红头.docx / 工作会议纪要-表尾.docx
- 局长办公会议纪要 → 局长办公会议纪要-红头.docx / 局长办公会议纪要-表尾.docx
- 党组会议纪要 → 党组会议纪要-红头.docx / 党组会议纪要-表尾.docx
- 会议纪要 → 工作会议纪要-红头.docx / 工作会议纪要-表尾.docx（默认走工作）

#### 新增函数

| 函数 | 用途 |
|------|------|
| `is_minutes_type(doc_type)` | 判断是否为会议纪要类型 |
| `update_wps_textboxes(doc_elem, replacements)` | 替换WPS文本框（wps:txbx）中的占位符 |
| `update_minutes_footer_table(table, replacements)` | 替换纪要表尾表格中的占位符（含抄送） |
| `is_table_row_empty(row)` | 判断表格行是否为空 |
| `remove_empty_rows(table)` | 删除表格中所有空行（倒序遍历） |

#### 空行自动清理

**问题：** 密级、紧急程度、签发人等字段为空时，对应行替换后仍保留空行，排版不简洁

**解决方案：** 在 `update_red_header_table` 和 `update_footer_table` 替换完成后，自动调用 `remove_empty_rows` 清理所有空行

#### 红头/表尾占位符扩展

**改动函数：** `update_red_header_table`、`update_vml_shapes`、`update_minutes_footer_table`

新增支持的占位符：
- 【纪要编号】— 纪要编号（如〔2026〕第5号）
- 【印发单位】— 印发处室
- 【成文日期】— 日期（区分含"印发"的上下文，避免误替换）
- 【抄送】— 抄送单位（仅表尾）

#### generate_red_header_document 改造

- 纪要红头：自动判断模板类型（有表格走表格替换，无表格走WPS文本框替换），兼容两种格式
- WPS文本框红头：整体复制模板body内容，通过 `update_wps_textboxes` 替换占位符，并给正文标题加段前间距（4060 twips ≈ 7.16cm）避免遮挡
- 纪要表尾：支持多表格（出席列席表 + 抄送印发/印发表），逐个插入并替换
- 普通公文：逻辑不变

#### main函数新增纪要参数

| 参数 | 用途 |
|------|------|
| `--meeting-number` | 纪要编号（如〔2026〕第10号） |
| `--attendees` | 出席人员 |
| `--non-voting` | 列席人员 |
| `--print-unit` | 印发单位（默认：发文机关+办公室） |
| `--print-date` | 印发日期（默认：当天日期） |
| `--cc` | 抄送单位 |

#### 完成进度

| # | 格式类型 | 状态 |
|---|---------|------|
| 1 | 工作会议纪要 | ✅ 已完成 |
| 2 | 党组会议纪要 | ✅ 已完成 |
| 3 | 局长办公会议纪要 | ✅ 已完成 |
| 4 | 联合函件格式 | ❌ 暂不实现 |
| 5 | 联合下行文件格式 | ❌ 暂不实现 |
| 6 | 联合上行文件格式 | ❌ 暂不实现 |
| 5 | 联合下行文件格式 | ⏳ 等待模板 |
| 6 | 联合上行文件格式 | ⏳ 等待模板 |

---

## [2.3.2] - 2026-03-17

### 日期匹配优化

**问题：** 落款日期使用占位符格式（如 `2026年【X】月【X】日`）时，正则无法匹配，导致日期排版不生效（右对齐）

**根因：** 原正则 `^\d{4}年\d{1,2}月\d{1,2}日$` 只匹配纯数字，`\d{1,2}` 无法匹配 `【X】`

**解决方案：**
1. 扩展正则，兼容【】占位符格式：`^\d{4}年(?:\d{1,2}|【[^】]+】)月(?:\d{1,2}|【[^】]+】)日$`
2. 添加位置约束：仅在文档后60%的行中才进行日期匹配，避免正文中的独立日期（如 `2026年1月1日`）被误判为落款日期

**修改文件：** `scripts/format_document.py` 第472行

---

### Markdown表格转Word原生表格

**问题：** AI生成的公文中，附件回执等表格使用Markdown语法（`| 列1 | 列2 |`），但 `format_document.py` 无法解析，导致表格语法被当成纯文本输出

**解决方案：**
1. 新增 `parse_markdown_table()` 函数：从指定行开始解析连续的Markdown表格行，返回结构化的二维数组
   - 支持分隔行（`|---|---|`）自动跳过
   - 至少2行（标题行+数据行）才算有效表格
   - 自动按最大列数对齐
2. 新增 `add_word_table()` 函数：将解析结果生成Word原生表格
   - 使用 `Table Grid` 样式（带边框）
   - 首行自动加粗（表头）
   - 所有单元格使用公文字体
   - 表格后自动添加空段落保持间距
3. 在 `create_document()` 主循环中，优先检测 `|` 开头的行，匹配则走表格解析分支

**测试验证：**

测试内容：4行×4列的参会回执表

结果：
- 生成1个Word原生表格，4行×4列
- 表头行（首行）自动加粗
- 空单元格正确保留
- 分隔行被正确跳过

**修改文件：** `scripts/format_document.py`

---

## [2.3.1] - 2026-03-16

### 引用规范强制执行

**问题：** 2.3.0版本生成的公文，正文中没有引用标记（`[^1^]`、`[^2^]`），只在末尾列出【参考来源】

**根因分析：**
- SKILL.md 5.1节"写作思维"只问了3个问题，没有要求引用标记
- AI撰写时忽略了5.3节的引用规范
- 缺少生成前的检查机制

**解决方案：** 三层强制机制

#### 1. 新增5.0节"引用规范（必须遵守）"
- **5.0.1 正确做法**：撰写前先浏览素材 → 边写边添加引用标记
- **5.0.2 错误做法**：写完正文后才添加引用标记（容易遗漏）
- **引用类型对照表**：明确哪些素材需要引用，哪些不需要
- **重要警告**：本节规范必须严格执行

#### 2. 增强5.1节"写作思维"
- 新增第4个问题：**引用标记**
  - 如果是：必须严格按照5.3节规范添加引用标记
  - 如果不是：也要确认是否需要说明数据来源
- 新增关键警告：
  - 如果使用了政策依据、参考案例、数据支撑 → 必须添加引用标记
  - 格式：`[^1^]`、`[^2^]` 等（双^符号）
  - 不要忘记在末尾添加【参考来源】列表

#### 3. 新增6.3节"生成前检查（必须执行）"
三重检查机制：
```
□ 1. 引用标记检查
   - 如果使用了政策依据、参考案例、数据支撑
   - 检查正文中是否有 `[^N^]` 格式的引用标记
   - 如果没有 → 必须先补充引用标记再生成

□ 2. 参考来源检查
   - 检查末尾是否有【参考来源】列表
   - 检查是否使用了 `[分页符]` 标记（不要用 `---`）
   - 如果没有 → 必须先补充再生成

□ 3. 知识专库链接检查
   - 如果进行了多次搜索
   - 检查是否按地域/主题分类列出了所有知识专库链接
   - 如果没有 → 必须先补充再生成
```

**只有通过以上所有检查，才能执行format_document.py！**

### 移除所有emoji（用户要求）

**原因：** 用户希望skill更严肃一些，符合政府公文风格

**移除内容：**
- ⚠️ → "注意：" 或 "重要警告："
- ✅ → 移除或使用纯文字
- ❌ → 移除或使用纯文字
- 📊 → "素材统计"
- 📋 → "分类结果"
- 🔍 → "素材充足度评估"
- 🚨 → "重要警告"
- 📝 → "全文用途"

**效果：** SKILL.md更严肃、专业，符合政府公文风格

### 测试验证

**测试样例：** 《广东省低空经济发展促进条例（征求意见稿）》

**结果：**
- ✅ 正文中正确添加了`[^1^]`至`[^6^]`引用标记
- ✅ 末尾有完整的【参考来源】列表（6个参考文献）
- ✅ 有分类的知识专库链接（3个地域）
- ✅ Word文档生成成功

---

## [2.0.3] - 2026-03-09

### 密级和紧急程度自动空格

**问题：** 用户需要手动在密级和紧急程度中添加全角空格

**解决：** 代码自动为两字密级/紧急程度添加全角空格

**实现：**
```python
def add_fullwidth_space_if_needed(text):
    """为两字密级/紧急程度自动添加全角空格"""
    if text and len(text) == 2:
        if all('\u4e00' <= c <= '\u9fff' for c in text):
            return text[0] + '\u3000' + text[1]
    return text
```

**效果：**
- "内部" → "内　部"
- "特急" → "特　急"
- "公开" → "公开"（非两字不处理）

### SKILL.md 更新

**更新内容：**
- 明确说明两个脚本的分工（format_document.py vs template_generator.py）
- 更新命令格式（移除过时的 --type 参数）
- 添加密级和紧急程度自动空格说明

---

## [2.0.2] - 2026-03-09

### 函件支持

#### 新增模板
- ✅ **上行文**：
  - `上行文件格式-红头.docx`
  - `上行文件格式-表尾.docx`
  - 特点：红头第二行有【签发人】占位符
- ✅ **函件**：
  - `函件（普通）格式-红头.docx`
  - `函件（普通）格式-表尾.docx`
  - 特点：发文机关使用VML图形绘制（非表格单元格）

#### VML图形支持
- **问题**：函件红头的发文机关使用绘图工具（VML图形）绘制，不在表格单元格中
- **解决**：添加 `update_vml_shapes()` 函数，支持替换VML图形中的占位符
- **实现**：遍历文档所有XML文本节点，替换包含占位符的节点

#### 函数更新
- **`update_vml_shapes(doc, replacements)`**：新增函数
  - 替换VML图形中的占位符
  - 支持占位符：【发文机关】、【发文字号】、【密级】、【紧急程度】

#### 当前支持的文种
| 文种 | 类型 | 模板 | 状态 |
|------|------|------|------|
| 请示 | 上行文 | 上行文件格式 | ✅ 完成 |
| 报告 | 上行文 | 上行文件格式 | ✅ 完成 |
| 通知 | 下行文 | 下行文件格式 | ✅ 完成 |
| 通报 | 下行文 | 下行文件格式 | ✅ 完成 |
| 函 | 函件 | 函件（普通）格式 | ✅ 完成 |
| 复函 | 函件 | 函件（普通）格式 | ✅ 完成 |

---

## [2.0.1] - 2026-03-09

### 模板系统重构

#### 占位符格式变更
- **旧格式**：`{{占位符}}`（易被Word拆分成多个run）
- **新格式**：`【占位符】`（中文方括号，不易拆分）

#### 替换逻辑优化
- **旧逻辑**：合并所有run文本 → 替换 → 只保留第一个run
  - 问题：会把固定文字和内容都设置成第一个run的格式
- **新逻辑**：遍历每个run → 只替换包含占位符的run → 保留每个run自己的格式
  - 优点：固定文字和内容可以有不同的字体

#### 模板目录整合
- **旧目录**：
  - 用户工作目录下的 `templates/`
  - `~/.openclaw/skills/official-doc-writer/templates/`（旧模板）
  - `~/.openclaw/skills/official-doc-writer/templates_red_header/`（旧红头模板）
- **新目录**：
  - `~/.openclaw/skills/official-doc-writer/templates/`（统一管理）
  - 路径：`Path(__file__).parent.parent / "templates"`

#### 当前支持的模板
- ✅ **下行文**：
  - `下行文件格式-红头.docx`
  - `下行文件格式-表尾.docx`

- ⏳ **待添加**：
  - 上行文件格式-红头.docx
  - 上行文件格式-表尾.docx
  - 函件（普通）格式-红头.docx
  - 函件（普通）格式-表尾.docx

### 下行文模板规范

#### 红头表格（下行文件格式-红头.docx）

| 行 | 内容 | 字体 | 字号 | run数 |
|----|------|------|------|-------|
| 行0 | `【密级】`<br>`【紧急程度】` | 黑体 | 16pt | 1+1 ✅ |
| 行1 | `【发文机关】文件` | 方正小标宋简体 | 34pt | 1 ✅ |
| 行2 | `【发文字号】` | 仿宋_GB2312 | 16pt | 1 ✅ |

#### 结尾表格（下行文件格式-表尾.docx）

| 行 | 单元格 | 内容 | 字体 | 字号 | run数 |
|----|--------|------|------|------|-------|
| 行0 | 0, 1, 2 | `公开方式：`<br>`【公开方式】` | 方正黑体_GBK / 仿宋_GB2312 | 16pt | 1+1 ✅ |
| 行2 | 0 | `抄送：` | 方正仿宋_GB2312 | 14pt | 1 ✅ |
| 行2 | 1, 2 | `【抄送】` | 方正仿宋_GB2312 | 14pt | 1 ✅ |
| 行3 | 0, 1 | `【发文机关】办公室` | 仿宋_GB2312 | 14pt | 1 ✅ |
| 行3 | 2 | `【印发日期】印发` | 仿宋_GB2312 | 14pt | 1 ✅ |

### 占位符列表

```
【发文机关】
【发文字号】
【密级】
【紧急程度】
【公开方式】
【抄送】
【成文日期】
【印发日期】
```

---

## [2.0.0] - 2026-03-09

### 架构变更

- **三段式架构**： 红头 + 正文 + 尾表
  - 癮通格式：只保留正文排版核心代码（format_document.py）
  - 红头格式：使用模板表格 + 正文排版（template_generator.py）
  - 清晰分离，易于维护

### 代码变更
- **format_document.py** (v2.0.0)
  - 删除所有红头相关代码（13个函数）
  - 只保留普通格式排版核心
  - 文件大小：41KB → 17KB
  - 行数： ~1300 → ~450
  
- **template_generator.py** (保持不变)
  - 从 format_document.py 导入核心排版函数
  - 负责红头和尾表的生成

### 功能改进
- **is_signing_entity()** 布局识别优化
  - 巻加落款单位识别规则：
    - 以"广东省"开头
    - 长度小于30
    - 以机构名结尾（局、厅、委等）
    - 不包含具体场所（会议室、办公室等）
    - 不以句号结尾

### 删除的函数（format_document.py）
- add_red_footer_table()
- add_red_header_shangxing_table()
- add_red_header_xiaxing_table()
- add_red_header_han_table()
- add_red_header_jiyao_table()
- add_red_header_lianhe_shangxing_table()
- add_red_header_lianhe_xiaxing_table()
- add_red_header_lianhe_han_table()
- add_red_header_dangzu_jiyao_table()
- add_red_header_gongzuo_jiyao_table()
- remove_table_borders()
- set_cell_bottom_red_border()

### 保留的核心函数（format_document.py）
- 配置相关： load_format_config(), get_font_config(), get_page_margin(), get_next_version()
- 格式化相关： set_run_font(), set_paragraph_format(), set_outline_level()
- 行类型判断： get_line_type(), strip_markdown_heading(), strip_markdown_bold()
- 辅助判断： is_attachment_line(), is_signing_entity(), is_contact_info(), is_recipient()
- 输入处理： validate_input(), fix_reference_format()
- 文档创建： create_document(), main()

