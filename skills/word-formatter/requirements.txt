# Word 智能排版 Skill 依赖
# python-docx: Word 处理核心库
# python-frontmatter: Markdown frontmatter 解析
# PyYAML: YAML 解析
# reportlab: PDF 生成（可选）
# pdfplumber: PDF 解析（可选）
# libraoffice: .doc 转换工具（系统工具）

# 核心依赖
python-docx>=0.8.11
python-frontmatter>=1.0
PyYAML>=6.0

# 可选依赖（PDF 支持）
reportlab>=4.0
pdfplumber>=0.10

# 可选依赖（高级功能）
Pillow>=10.0
cryptography>=41.0
