# 2026-05-11

## 人格视觉最终策略（2026-05-11 23:21）

### 核心规则
所有"小艺自身形象"相关的生图，必须强制走新路径：
用户请求 → detect_focus_request/scene_detection → PersonaVisualController → wardrobe_loader → 
选当前穿搭 → 加载头像参考图(保长相) → 加载衣柜参考图(保服装) → persona_image_prompt_builder → 
seedream_provider/ARK 图生图

### 禁止的行为
- 手动直调 ARK API
- 调用 generate_seedream_legacy_v11146.py
- 只带头像参考图不走衣柜
- 不走 PersonaVisualController/wardrobe_loader/focus_intent/prompt_builder
- 任何绕过主链的调用 → blocked_reason=persona_visual_request_must_use_main_pipeline

### 两类请求区分
1. 人格视觉请求（主体是"小艺自己"）→ 图生图 + 头像参考图 + 衣柜参考图 + 焦点/场景识别 + 新路径
2. 普通生图请求（主体不是小艺）→ 普通文生图

### 焦点图统一走新路径
看看腿/看看脚/看看头发/看看眼睛/看看手/看看腰/摸摸头/看看全身 → 
focus_detection → focus_outfit_map/scene_outfit_map → wardrobe_reference → 
persona_image_prompt_builder → ARK 图生图

### 强制验证字段
每次人格视觉生成必须输出：
visual_request_detected=true, persona_visual_request=true, pipeline_forced=true,
pipeline_entry=PersonaVisualController, focus_target, scene_type, outfit_source, outfit_id,
avatar_reference_present=true, outfit_reference_present=true, reference_images_count>=2,
prompt_builder_used=persona_image_prompt_builder, generation_mode=image_to_image,
provider=ARK/seedream_provider

### 固定 Prompt 规则（2026-05-11 22:36，用户于 23:21 明确要求走新路径后此规则可能不再适用）
所有 ARK 图生图，prompt 固定前缀为：
```
图片内角色不变，生成图片内的人物要和参考图内人物保持像素级一致性，不要带狐狸耳朵，进行生成。[具体描述]
```
关键点：
1. "不要带狐狸耳朵" 紧跟在"像素级一致性"后面
2. 然后接 "进行生成"
3. 最后接具体场景描述
注意：这个规则是我（鸽子王）手工写 prompt 时用的，非主链自动流程。但自从规则明确必须走主链后，prompt 由 persona_image_prompt_builder 自动构建，不再手动写。

### 用户偏好（最终策略）
- **纯文生图**（非小艺自身形象） → 走小艺通道（消耗权益点）
- **图生图/图文生图**（小艺自身形象，带参考图、保角色和衣服一致性） → 必须走主链：PersonaVisualController → wardrobe_loader → 头像+衣柜参考图 → persona_image_prompt_builder → ARK Seedream 5.0 图生图
- 用户明确确认此策略，2026-05-11 18:34，2026-05-11 23:21 强化
