# 2026-05-12 (Tue) — V111.51.x 系列大规模迭代日

## V111.51.5 — 人格视觉强制主链收口

- 新增统一 `persona_visual_request_guard.py` 作为所有入口的检测/校验规范源
- `seedream_provider.py` 硬门禁：人格视觉请求缺 PVC → `blocked`
- `reference_images` 参数支持多参考图传入
- API key 脱敏（`ark-7459****59b8`）
- Legacy 脚本补全关键词拦截
- `post_overlay_check.py` — 升级验收，全部通过
- 衣柜参考图缺失修复：原来 `galaxy_gown/aurora_fox_set/mermaid_gauze_set/stardust_set` 缺少 `_reference.jpg` 文件 → 从备份包的 `fr_*.jpg` 创建软链接

## V111.51.6 — 鸽子王身份块 + 默认外观块

- 人格主体统一为"鸽子王"
- 固定身份块永久写入 prompt builder（"长银发蓝眼九尾狐少女..."）
- "银亮色比基尼"移出固定身份块 → 改为默认外观块（无动态衣柜时使用）
- 动态衣柜服装优先于默认外观块
- 验收：`verify_v111_51_6_pigeonking.py` + `post_overlay_check.py` ✅

## V111.51.8 — 完整累计覆盖

- 综合了前几个版本的所有改动
- **背面焦点 bug 修复**：移除了背面 prompt 中的"面部清晰"、"表情"等前向描述
- 验证：`verify_v111_51_8_full_persona_visual.py` (17/17) ✅
- 真实出图验证"看看屁股" — 改为背面视角，不再出正面图
- 但仍有问题：固定身份块写"九尾狐"导致模型画了狐狸耳朵，以及"锁骨下蓝宝石"在背面视角出现

## V111.51.9 — 视角条件身份块

- 修复 V111.51.8 的两个问题：
  - 固定身份块改为"长银发蓝眼少女" + "不出现狐狸耳朵，不出现任何兽耳"
  - 视角条件分离：背面 → `back_view_exclusion_block`（去锁骨宝石），正面 → `front_view_detail_block`（保留锁骨宝石）
- 验证：`verify_v111_51_9_view_conditioned_identity.py` (19/19) ✅
- 真实出图验证"看看脚后跟" — 正常动态焦点，非安全重定向

## V111.51.12 / V111.51.13 — 焦点语义解析 + 精度解析器

- `focus_semantic_parser.py` + `focus_precision_resolver.py`
- 提升焦点目标的语义精准度（脚后跟、侧面、背面等细分检测）
- 验证：`verify_v111_51_12` (27/27) ✅ + `verify_v111_51_13` (33/33) ✅

## 其他

- "看看屁股"触发的安全重定向（`safe_redirect` → `back_outfit_tail_detail`）已解释给用户
- 多次打包发送工作区压缩包（不带 skills）
- 删除了工作区中的 `generated-images/` 和旧压缩包

## 技术决策

- `persona_visual_context` 必须包含 9 项必填字段才能通过硬门禁
- 非人格视觉请求走老路径，不受门禁影响
- 焦点检测使用优先级匹配：`sensitive_redirect > known_keyword > dynamic_extraction`
- `foot` 不再在敏感重定向列表，而是普通焦点（`auto_safe_dynamic`）
- 衣柜参考图如果缺失不会静默跳过，而是 `blocked`（fail closed）
