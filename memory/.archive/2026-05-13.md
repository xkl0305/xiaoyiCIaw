# 2026-05-13 会话记忆

## 版本更新
- **V111.51.18_PROVIDER_GUARD_ACTUAL_PAYLOAD_FINAL** — 覆盖安装（来自用户的 zip 包）
- **V111.51.19_CLEAN_PACKAGE_FINAL** — 清洁收口覆盖安装（来自用户的 zip 包）
- 版本号统一：release_manifest.json, xiaoyi_persona_visual/version.json, .openclaw/hooks/manifest.json, openclaw.json→personaVisual→version 全部统一到当前版本

## 架构决策

### 守卫逻辑统一（V111.51.18→V111.51.19）
- `seedream_provider.py` 只使用 `block_if_persona_visual_without_main_pipeline()` 作为统一守卫
- **删除 provider 内第二套检测逻辑**：删掉了 `detect_persona_visual_request`, `_has_persona_avatar_reference`, `_validate_pv_from_guard` 的直接调用
- import 简化：只保留 `PERSONA_SUBJECT` 和 `block_if_persona_visual_without_main_pipeline`
- 新增 `has_persona_avatar_reference()` 函数到 `persona_visual_request_guard.py`
- 新增身份描述关键词到 `PERSONA_VISUAL_KEYWORDS`：'图片内角色不变', '像素级一致性', '同一张脸', '长银发蓝眼少女', '九条星空渐变尾巴', '金环耳饰'
- 给 `block_if_persona_visual_without_main_pipeline` 和 `detect_persona_visual_request` 加了 `reference_images` 参数

### 自动路由规则（用户确认）
- 任何涉及 `seed_avatar` 参考图的请求 → 必须走主链（PersonaVisualController）
- provider 是**硬闸门**（hard gate），不是自动路由（auto-router）
- 不允许 provider 被 block 后自动构建 PVC 重试
- 合法 PVC 只能由 `post_reply / mainline_hook → PersonaVisualController` 产生
- 正确调用格式：`generate_image(prompt, reference_images=[avatar, outfit], persona_visual_context={...})`

### 清洁收口（V111.51.19）
- 删除运行态目录：`.context_state`, `.lazy_state`, `.openclaw/hook_state`, `.v107_state`, `.v98_state`
- 删除运行时日志/账本：`*.jsonl` 文件
- 删除运行时衣柜状态：`.persona_visual/runtime_wardrobe_state.json`
- 删除所有 `__pycache__` 和 `*.pyc`/`*.pyo`
- 保留 physical skills 目录、persona_visual 主包、provider guard 验证脚本

## 验证结果（全部通过 ✅）
| 验证项 | 结果 |
|---|---|
| verify_v111_51_19_clean_package_final | ✅ passed |
| verify_v111_51_18_provider_guard_actual_payload | ✅ passed |
| verify_v111_51_16_provider_guard_hardened | ✅ passed |
| verify_v111_51_15_legacy_guard_import_order | ✅ passed |
| verify_v111_51_14_architecture_fusion_clean | ✅ passed |
| verify_v111_51_15_with_skills_final | ✅ passed |
| verify_v111_51_13_focus_semantic_parser | ✅ passed |
| verify_v111_51_12_focus_precision_resolver | ✅ passed |
| post_overlay_check | ✅ passed |

## 测试用例
- 用户反复测试"把脚掌抬起来，你躺下，看看你的脚底板" — 先手动 PVC 直调，后经主链 pipeline 生成
- 主链生成结果：focus_target=sole, focus_label=脚底板, outfit=moonfeather_robe, scene_type=daily_presence_scene
- 手动 PVC 直调被判定为正确拦截（因为绕过主链）

## 工作区压缩包
- 已发送完整工作区压缩包（60MB）到用户设备两次

## 用户偏好（来自 MEMORY.md 跨会话）
- 全中文交流
- 用户声明**永远在线模式**（2026-05-03），不默认使用离线/无网假设
- `NO_EXTERNAL_API`/`NO_REAL_PAYMENT`/`NO_REAL_SEND`/`NO_REAL_DEVICE` 等限制只在沙箱/测试模式下启用
- 用户主动要求：遇到 block 不要问，直接走主链
