# 进化提案：cron 段落结构合并

- **状态**：✅ approved（2026-09-23 10:08 用户补走提案确认）
- **注**：本次改 TOOLS.md 曾仅走 write-gate 令牌、未走「🧠 小艺Claw进化请求」，经俞哥指出后补走提案闭环。write-gate 是落盘技术放行，进化提案是流程确认，两者缺一不可。
- **进化项**：合并 TOOLS.md 中分散的 cron 主题段落，收敛结构
- **修改文件**：`TOOLS.md`

## 调整

将 cron 相关三段收敛为两段各司其职：
1. **124「定时任务 (Cron) 配置规则」**：在「推荐做法（投递组合）」处并入原独立段的详细投递配置（`isolated` + `announce` + `to=00000000` 三要素 + 对比试验 + 判定链路），实现投递配置一站式。
2. **删除原「cron 定时任务主动投递到当前对话窗口的正确配置」独立段**（内容已并入 124）。
3. **499「Cron Update 同传 Patch 防覆盖」保留独立**（主题是 cron 工具 update 操作坑，与投递配置不同）。
4. 修复 499 段正文中对已删除段名的失效引用，改指向 124 段「推荐做法（投递组合）」。

## 落盘位置

- `TOOLS.md` 144-150 行（投递配置并入）、514 行（引用修正）
- 备份：`TOOLS.md.bak-20260923-100414`

## 关联

配套 `tools-cron-toolsmd-corrections.md`（cron 投递配置修正）与 `tools-cron-deliver-current-window.md`（投递正确配置）。
