# 进化提案：修正 xiaoyi-channel 表格"只支持两列"错误结论

- **状态**：✅ approved（2026-09-24 用户确认）
- **进化项**：修正 TOOLS.md「xiaoyi-channel 表格渲染坑点」的错误结论
- **修改文件**：`TOOLS.md` 358 段

## 经验规则

1. **推翻**"只支持两列（标题+内容）、多列在手机端丢弃"——**实测支持多列**：3 列/4 列表格端侧均正常渲染（2026-09-24 用户核实）。
2. 保留并修正"单元格内避免裸 `|` 管道符"：多余 `|` 会被 markdown 当作**新列分隔符**导致列结构错位（而非"后列丢失"）；单元格内分隔用 `·`/`/`。
3. 更新示例为多列表格写法。

## 落盘位置

- `TOOLS.md` 358 段「xiaoyi-channel 表格渲染坑点」全文修正
- 备份：`TOOLS.md.bak-20260924-073454`

## 关联

教训：长期依赖一条固化但过时的记录（两列限制），未做实测定，直到用户多次尝试多列才推翻。凡涉及端侧渲染能力，应以**最新实测**为准，不盲信历史固化。
